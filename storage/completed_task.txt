The quiz feature for teachers (guru) has been fully implemented, including CRUD operations for quizzes and questions, with corresponding routes, controller methods, and views (`index.blade.php`, `show.blade.php`, `edit.blade.php`). Dynamic question management is included in the edit view. The routes for quizzes are now defined individually instead of using a resource group. The action buttons on the quiz listing page for teachers now have the same styling and include Heroicons, matching the appearance of the buttons in the admin mata pelajaran management page. The `RouteNotFoundException` for `guru.kelas.problems.edit` was resolved by removing the `->shallow()` modifier from the `Route::resource('kelas.problems', ProblemController::class)` definition in `routes/web.php`. This ensures that the nested resource routes for problems correctly include the `kelas` parameter, aligning with the `ProblemController`'s method signatures. The `UrlGenerationException` for `guru.kelas.problems.edit` was resolved by explicitly defining the parameter names for the `kelas.problems` resource route in `routes/web.php`. The `parameters` method was used to map `'problems'` to `'problem'` and `'kelas'` to `'kelas'`, ensuring the route correctly expects the `kelas` parameter. The buttons on the `/guru/problems` page have been styled to match the buttons on the `/admin/users` page, including the use of Heroicons and consistent Tailwind CSS classes for "Edit", "Delete", "Submissions", and "Discussions" actions. The action buttons are now wrapped in a `div` with `flex items-center space-x-2` for proper alignment.